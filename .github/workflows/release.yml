name: release

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Yarn Release
        run: |
          GH_TOKEN=${{ secrets.GH_TOKEN }}
          yarn release
